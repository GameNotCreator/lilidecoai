# Application
DEMO_MODE=true
NEXT_PUBLIC_APP_URL=http://localhost:3000
APP_SESSION_SECRET=replace-with-at-least-32-random-characters

# MongoDB (local or Atlas)
MONGODB_URI=mongodb://127.0.0.1:27017/lilidecoai
MONGODB_DB=lilidecoai

# Vercel Blob legacy/local token (new Vercel projects use OIDC automatically)
BLOB_READ_WRITE_TOKEN=

# Temporary room-photo retention and Vercel upload limit
MAX_UPLOAD_BYTES=4000000
ROOM_RETENTION_HOURS=24
CRON_SECRET=replace-with-a-random-cron-secret

# OpenAI Image — server only
OPENAI_API_KEY=
OPENAI_MODEL=gpt-image-2
OPENAI_QUALITY=medium
OPENAI_MAX_COST_USD=0.25
OPENAI_BASE_URL=https://api.openai.com/v1

# Konnect — server only
KONNECT_API_KEY=
KONNECT_WALLET_ID=
KONNECT_BASE_URL=https://api.konnect.network/api/v2
